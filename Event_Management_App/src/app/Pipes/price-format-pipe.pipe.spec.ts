import { PriceFormatPipePipe } from './price-format-pipe.pipe';

describe('PriceFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
