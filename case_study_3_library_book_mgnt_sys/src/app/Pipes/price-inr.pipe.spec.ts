import { PriceInrPipe } from './price-inr.pipe';

describe('PriceInrPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceInrPipe();
    expect(pipe).toBeTruthy();
  });
});
